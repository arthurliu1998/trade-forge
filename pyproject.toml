[project]
name = "quantforge"
version = "0.1.0"
description = "Quantitative trading system with LLM intelligence layer"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.21"]
